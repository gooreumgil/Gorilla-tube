extends layouts/main
include mixins/videoBlock

block content
  .home-videos
    each item in videos
      +videoBlock({
        id: item.id,
        title: item.title,
        views: item.views,
        fileUrl: item.fileUrl
      })
        //- h4= item.description
        //- h5= item.views
        //- video(src=item.videoFile, controls=1) 